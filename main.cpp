#include <iostream>
#include<vector>
#include<map>
#include<string>
#include"jindong-1.h"
#include"printVector.h"
using namespace std;

int main(){
//    vector<int> stkLen{1, 3};
//    vector<int> count{4, 4};
//    vector<int> used(2, 0);
//    int len = 4;
//    vector<vector<int>> pUsed;
//    dfs(stkLen, count, used, 0, len, pUsed);
//    cout << pUsed.size() << endl;
//    for(auto vec : pUsed){
//        printVector(vec);
//    }

    jindong1();
    return 0;
}